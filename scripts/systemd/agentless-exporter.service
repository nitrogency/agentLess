[Unit]
Description=Agent< Log Exporter
After=network.target

[Service]
Type=oneshot
User=root
Group=root
WorkingDirectory=/opt/agentless

# Load environment variables from .env files
# Load secrets first (DB key, session secrets)
EnvironmentFile=/etc/agentless/secrets.env
# Load app configuration second (paths, settings)
EnvironmentFile=/opt/agentless/.env

ExecStart=/opt/agentless/bin/exporter

PrivateTmp=true
NoNewPrivileges=true
ProtectSystem=strict
ProtectHome=true
ReadWritePaths=/var/log/agentless-export /var/lib/agentless

StandardOutput=journal
StandardError=journal
SyslogIdentifier=agentless-exporter

TimeoutSec=300
CPUQuota=25%
MemoryMax=256M

[Install]
WantedBy=multi-user.target
